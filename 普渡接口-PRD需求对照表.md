# 普渡接口能力矩阵 × PRD 需求对照表

| 项目 | 园区智能机器人中央管控平台 |
| :--- | :--- |
| 对照依据 | 客户 PRD V1.0（2026-08-27 评审稿）× 普渡开放平台官方文档 305 篇 |
| 文档快照 | 2026-08-28 抓取自 <https://open.pudutech.com>，含云开放接口 156 篇、私有云 138 篇、PUDU OS SDK 11 篇 |
| 接口清单 | 180 个 API、57 个回调通知（其中云开放接口 32 个） |
| 编制目的 | 作为《需求澄清件》附件，逐条确认可实现性，供客户与我方共同签认 |
| 状态 | **待客户确认** |

---

## 一、结论摘要

| 判定 | 需求点数 | 占比 | 说明 |
| :--- | ---: | ---: | :--- |
| ✅ 可实现 | 11 | 50% | 普渡接口直接支持，或属平台侧自有逻辑 |
| ⚠️ 降级实现 | 7 | 32% | 接口能力弱于 PRD 描述，需下调验收口径；或依赖我方自研设备接口 |
| ❌ 不可实现 | 4 | 18% | 普渡侧无对应能力，需变更方案或移出范围（均为 5.4 视频监控条目） |
| **合计** | **22** | **100%** | 对应 PRD 第 5 章 F1-1 ~ F7-2 全部需求点 |

> **口径说明**：本表初稿曾将 5.5 视频 AI 整章（3 项）判为不可实现。经确认
> **PRD 中归属「国科」的水下 / 爬壁 / 管道 / 灭虫四类机器人即我方自研设备**后，
> 「管道缺陷识别」「水面漂浮物识别」两个 AI 场景的视频源来自我方设备、不受普渡限制，
> 且 F5-2 / F5-3 属平台侧逻辑，故上调判定。**不可实现项现已收敛为 5.4 视频监控 4 条。**

**三项必须立即决策的事项**（本表已于 2026-08-28 复核，两处结论按 PdCoreSDK 实际能力
作了据实修正，见 5.3.3 与 5.4 节的「重要更正」段）**：**

1. **普渡设备无视频能力（PRD 5.4 全章 4 个需求点）。** 普渡**云端**开放平台
   （公有云 156 篇 + 私有云 138 篇）无任何视频流、录像、云台接口，
   这是验收标准第 2、4 条的直接依据。
   *注：机上 PdCoreSDK 有相机预览/拍照接口但无推流能力，详见 5.4 节「重要更正」。*
   **但视频与 AI 并非整体不可做** —— 我方自研的 4 类特种设备不受此限制，
   叠加方案 A 的园区固定点位摄像头即可覆盖 PRD 全部 6 个 AI 场景（详见第四章与第八章）。
2. **远程急停不可实现。** 官方错误码明确要求「前往机器人所在地点解除急停」，无远程下发接口。
   PRD 5.3.2「5 秒内停止运动」无法承诺，且降级方案（暂停任务）覆盖机型极窄。
3. **设备型号未确认，全表存在返工风险。** 普渡每个接口都有机型白名单，PRD 3.1 只写品牌未写型号。
   本表按「最可能型号」推定，**型号一旦不符，判定结论需整表复核**。

---

## 二、判定图例

| 记号 | 含义 |
| :--- | :--- |
| ✅ | 可实现。有明确接口支撑，验收标准可原样保留 |
| ⚠️ | 降级实现。能做但达不到 PRD 原描述，需修改验收口径（本表给出建议口径） |
| ❌ | 不可实现。普渡侧无此能力，需变更方案、更换设备或移出一期 |
| 🔍 | 依赖型号确认。结论随实际机型变化 |

---

## 三、逐条对照

### 5.1 设备接入与管理

#### F1-1 第三方 API 统一适配层 — ✅ 可实现

| PRD 要求 | 普渡对应能力 | 判定 |
| :--- | :--- | :--- |
| 对各品牌 API 协议转换，统一输出标准状态/控制接口 | 普渡侧提供完整 REST API + 回调，适合封装为 Adapter | ✅ |
| 配置页面录入 API 地址、AppKey、AppSecret、SN 完成绑定，无需改代码 | 鉴权为 HMAC-SHA1 应用签名，AppKey/AppSecret 参数化可行 | ✅ |
| 心跳检测，第三方不可用时标记离线 | `run_state` 含 `OFFLINE`；另有 `robotStatus` 在离线变更回调（机型 ALL） | ✅ |
| 第三方 API 断连后 30 秒内显示离线 | 可由适配层自行判定（请求超时 + 回调静默超时双重判据） | ✅ |

**实现要点**
- 网关：`https://open-platform.pudutech.com/pudu-entry`（国内生产节点）
- 签名串：`x-date\nMETHOD\nAccept\nContent-Type\nContentMD5\nPath`，Path 含字典序排序的 query
- 请求头：`Authorization: hmac id="{AppKey}", algorithm="hmac-sha1", headers="x-date", signature="{sig}"`
- 参见 `pudu-docs/zh/云开放接口/开发指南/003-鉴权接入.md`

**风险**
- ⚠️ **AppKey/AppSecret 需向普渡申请并通过审批**，审批周期未知，是项目启动的前置依赖。
- ⚠️ **回调地址在申请 AppKey 时一并填写**。若后期无法自助修改，测试环境与生产环境切换将受限，
  需向普渡确认能否配置多个回调地址。

---

#### F1-2 设备台账与分组 — ⚠️ 降级实现

| PRD 要求字段 | 普渡是否提供 | 说明 |
| :--- | :--- | :--- |
| 设备名称 | ✅ | `nickname` |
| 种类 / 型号 | ✅ | `product_code`，枚举见 `开发指南/FAQ/007-机器类型.md`（26 种机型） |
| 序列号 | ✅ | `sn`、`mac` |
| 品牌 | ✅ | 由我方台账维护 |
| 安装位置 | ✅ | `shop_id` / `shop_name`（门店维度） |
| **投入使用日期** | ❌ | 普渡 API 不返回，需我方台账手工录入 |
| **质保状态** | ❌ | 普渡 API 不返回，需我方台账手工录入 |
| 分组管理 | ✅ | `GET /open-platform-service/v1/robot/group/list` 可读普渡侧分组；自定义分组由我方维护 |
| 启用 / 停用 | ✅ | 平台侧本地状态位实现 |

**建议验收口径**：投入使用日期、质保状态改为「平台支持手工录入与维护」，不表述为从设备读取。

---

### 5.2 实时状态监控

#### F2-1 设备总览大屏 — ⚠️ 降级实现 🔍

| PRD 要求 | 普渡对应能力 | 判定 |
| :--- | :--- | :--- |
| 统计卡片：总数/在线/离线/作业中/故障/今日告警 | `run_state`(OFFLINE/DISABLE/BUSY/IDLE) 可聚合；故障数由 `robotErrorNotice` 累计 | ✅ |
| 地图标注每台设备位置 | `GET /open-platform-service/v1/robot/get_position`（机型 ALL）<br>`GET /map-service/v1/open/map` 取地图详情 | ✅ |
| 图标颜色区分状态 | `run_state` 四态可映射 | ✅ |
| 详情卡片：当前任务、电量、**续航预估**、位置、最近告警 | 电量 `battery` ✅；**续航预估 `remain_time` 仅 T300 支持** ⚠️ | ⚠️ |
| 状态刷新 ≤ 5 秒 | 靠回调 `notifyRobotStatus`（机型 ALL，工作/电量/移动/在离线变化即推） | ✅ |

**关键差异：位置刷新在清洁机上是轮询，不是推送**

| 机型 | 位置获取方式 | 实时性 |
| :--- | :--- | :--- |
| FlashBot 系列、BellaBot Pro、T300/T150/T600 | `position_command` 触发 + `notifyRobotPose` 推送（带毫秒级 `notify_timestamp` 可做乱序过滤） | 推送，可达 ≤5 秒 |
| **CC1 / CC1 Pro / MT1 / MT1 Vac / MT1 Max（清洁机）** | **仅能轮询 `get_position`，无位置推送回调** | **取决于轮询频率，受 QPS 限制** |

**建议验收口径**：
- 「续航预估」标注为「仅部分机型（T300 系列）提供，其余机型显示电量百分比」
- 「位置刷新 ≤5 秒」标注为「支持位置推送的机型 ≤5 秒；清洁机型采用轮询，刷新周期以普渡 QPS 限制为准」

**待普渡确认**：开放平台 QPS / 调用频次限制（决定清洁机位置轮询能做到多快）。

---

#### F2-2 设备详情与运行参数 — ⚠️ 降级实现 🔍

**清洁类（CC1 / CC1 Pro / MT1 / MT1 Vac / MT1 Max）— 覆盖度最好**

接口：`GET /cleanbot-service/v1/api/open/robot/detail`

| PRD 要求 | 普渡字段 | 判定 |
| :--- | :--- | :--- |
| 清洁模式 | `clean.mode`（1 手动 / 2 自动）、`last_mode` | ✅ |
| 已清洁面积 | `clean.result.area`、`task_area` | ✅ |
| 水箱液位（清水/污水） | `rising`（上水箱余量）、`sewage`（污水箱余量） | ✅ |
| 任务进度 | `clean.result.percentage`、`remaining_time` | ✅ |
| 耗水量 | `clean.result.cost_water` | ✅ |
| **垃圾箱状态** | ❌ 无此字段。仅 `robotErrorNotice` 有「污水箱已满」「清水箱已空」等告警事件 | ⚠️ |
| **刷盘转速** | ❌ 无此字段 | ❌ |

**物流 / 配送类**

| PRD 要求 | 普渡字段 | 判定 |
| :--- | :--- | :--- |
| 当前任务 | `GET /open-platform-service/v1/robot/task/state/get`、各任务 list 接口 | ✅ |
| 途经点 | 配送任务返回目的地列表 | ✅ |
| 预计完成时间 | ⚠️ 无直接字段，可由任务进度估算 | ⚠️ |
| **载重状态** | ❌ **全文检索「载重」命中 0**。仅私有云「订单到人」模块有货物 `weight`（g），与本项目无关 | ❌ |

**巡逻类** — 见 F2-2 巡逻部分：`红外测温`、`360° 摄像头状态` 均无（详见 5.4 章）

**特种作业类（水下/爬壁/管道/灭虫）** — 非普渡设备，**由我方（国科）自研并提供接口**，
不受普渡开放平台限制，接口规格由我方另行出具（详见第八章）

**全设备通用字段**

| PRD 要求 | 普渡是否提供 | 判定 |
| :--- | :--- | :--- |
| 电量百分比 | ✅ `battery` | ✅ |
| 续航时间预估 | ⚠️ `remain_time` 仅 T300 | ⚠️ |
| **信号强度** | ❌ **全文检索 `signal` / `rssi` / `wifi` / 「信号强度」命中 0** | ❌ |
| **固件版本** | ⚠️ 仅出现在「开机自检」「电池健康」等日志类接口，非实时状态字段 | ⚠️ |
| **累计运行时长** | ⚠️ 无实时字段。可从 `/data-board/v1/brief/run` 取周期统计 `duration`(h)、`mileage`(km) | ⚠️ |

**PRD 原验收标准「参数值与第三方 API 返回一致，误差为 0」** —— 此条可保留，但前提是**先按上表删除
普渡不提供的字段**（刷盘转速、载重状态、信号强度），否则该条永远无法通过。

---

#### F2-3 故障与低电量告警 — ✅ 可实现

| PRD 要求 | 普渡对应能力 | 判定 |
| :--- | :--- | :--- |
| 电量 <20% 低电量告警，<10% 紧急告警 | `battery` + `notifyRobotPower` 电量变化回调；阈值判定在平台侧 | ✅ |
| 故障码上报并展示描述（需映射故障码字典） | `robotErrorNotice`（实时异常）+ `robotErrorWarning`（10 分钟延迟，机型 ALL）<br>故障码字典见 `开发指南/010-错误码.md`，**351 条**，可直接建映射表 | ✅ |
| 清洁机污水箱满、垃圾箱满告警 | `robotErrorNotice` 含 `MobileStationSewageFull`（污水箱已满）、`MobileStationCleanWaterEmpty`（清水箱已空）等；**「垃圾箱满」无对应故障码** | ⚠️ |
| 电量降至 19% 后 10 秒内告警 | 回调驱动，可满足 | ✅ |

**注意**：`robotErrorNotice`（实时）不支持全部机型，白名单为 FlashBot 系列、KettyBot Pro、
BellaBot Pro、T300/T150/T600、CC1/CC1 Pro/MT1 系列。**其余机型只能用 `robotErrorWarning`
（机型 ALL，但有 10 分钟延迟）**，无法满足「10 秒内展示」。此处判定依赖型号确认。

---

### 5.3 远程控制

#### F3-1 远程启停与任务派发 — ⚠️ 降级实现 🔍

| PRD 要求指令 | 普渡对应接口 | 支持机型 | 判定 |
| :--- | :--- | :--- | :--- |
| 开始作业 | 清洁 `task/exec` `type:3` + `clean.status:1`<br>配送 `delivery_action` `action:START`<br>巡航 `cruise_action` | 清洁：CC1 系列<br>配送：FlashBot 系列等<br>**巡航/运送：仅 PuduBot2** | ✅ |
| 暂停 | 清洁 `clean.status:3` ✅<br>巡航 `action:PAUSE` ✅<br>**配送 `action:PAUSE` 仅 T300 支持** ⚠️<br>**运送指令无 PAUSE** ❌ | 见左 | ⚠️ |
| 继续 | 巡航 `action:RESUME` ✅<br>**配送 `RESUME` 仅 T300** ⚠️ | 见左 | ⚠️ |
| 结束任务 | 清洁 `clean.status:4`（**取消前必须先暂停**）<br>配送 `CANCEL_ALL_DELIVERY`<br>巡航 `action:CANCEL` | — | ✅ |
| 返航充电 | `GET /open-platform-service/v2/recharge`<br>清洁 `task/exec` `type:1`(充电) / `type:5`(一键返航) / `type:6`(回返航点) | 回充：FlashBot 系列、T300 系列 | ✅ |
| 预设任务模板一键派发 | 清洁 `task_id` + `version` + `name` 指定已有任务；配送指定点位 | — | ✅ |
| 批量派发多台 | ⚠️ **无批量接口**，需平台侧循环单发 | — | ⚠️ |
| 状态回显（已下发/执行中/已完成/失败） | `notifyCleanStatus`、`TASK_STATUS`、各任务通知回调 | — | ✅ |

**建议验收口径**：
- 「暂停 / 继续」标注适用机型，非全设备通用
- 「批量派发 3 台全部成功」改为「平台支持批量选择，由适配层逐台下发，展示每台执行结果」
- 补充说明「清洁任务取消需先暂停」这一普渡侧时序约束

---

#### F3-2 紧急停止（急停）— ❌ 不可实现

**这是安全相关需求，必须书面澄清，不可含糊带过。**

| PRD 要求 | 普渡实际情况 |
| :--- | :--- |
| 平台下发急停指令 | **无任何下发急停的接口。** 全文检索「急停」/`Emergency` 命中 3 处，全部为**被动**能力：<br>① `robotEmergencyRecover` — 急停**恢复**回调（机器上物理按钮被按下后恢复时通知）<br>② 故障码 `CLEANBOT_EMERGENCY_STOP_SWITCH_TRIGGERED` — 急停开关**已触发**（状态上报）<br>③ 错误码 `CLOUD_OPEN_ROBOT_EMERGENCY_STOP` — 「机器人触发急停，无法响应请求，**请前往机器人所在地点解除急停后再试**」 |
| 设备 5 秒内停止运动 | **云端无法承诺。** 最接近的降级方案是「暂停任务」，但：<br>· 清洁机 `clean.status:3` 暂停 —— 仅停止清洁作业<br>· **配送 `PAUSE` 仅 T300 支持**<br>· **运送指令根本没有 PAUSE 动作**<br>暂停任务 ≠ 切断动力，安全语义与响应时间均不等价<br><br>**补充**：机上 PdCoreSDK 有 `globalStopNavigation(id)` 与 `INaviTask.stop()`，停止能力强于云端暂停，但需在机器上部署我方 App（见 F3-3 更正），且官方注明「调用前确认现场安全」，**仍非急停** |
| 急停二次确认、记录操作日志 | ✅ 平台侧可实现 |

**第三条错误码是官方对此事的直接表态：急停的解除必须由人到现场完成，普渡将急停设计为
本地安全机制，不开放远程控制。这符合工业安全惯例（急停回路通常为硬线设计，
不允许软件旁路），并非普渡的疏漏。**

**必须请客户在以下三项中书面选择：**

| 方案 | 内容 | 我方评估 |
| :--- | :--- | :--- |
| **A（推荐）** | 平台提供「远程暂停」按钮（非急停），文案与验收标准同步改为「暂停作业」；真正的急停依靠设备本体物理按钮 + 园区现场巡查制度 | 技术诚实，责任边界清晰 |
| **B** | 保留「急停」按钮命名，但验收标准改为「平台下发暂停指令并在 UI 标记急停中状态，实际制动由设备本体机制保证」 | 命名易误导操作人员，我方不建议 |
| **C** | 要求普渡开放远程急停接口 | 需普渡定制开发，周期与可行性均不可控；且可能违反其安全设计原则 |
| **D** | 基于机上 PdCoreSDK 自研 App，用 `globalStopNavigation` 实现「远程停止导航」 | 停止效果优于 A，但需逐机型申请交付包、每台部署、脱离普渡质保；**且仍不是急停**。建议列为二期评估项 |

**责任提示**：若客户坚持保留「点击急停后设备 5 秒内停止运动」的验收标准，我方无法承接该条，
因其依赖的接口在普渡侧不存在。此点需在合同或澄清件中明确。

---

#### F3-3 远程导航与定点移动 — ❌ 部分不可实现

| PRD 要求 | 普渡实际情况 | 判定 |
| :--- | :--- | :--- |
| **地图上点击任意目标点，下发「移动到此处」** | **云端 Open API 无 goto / navigate / 任意坐标导航接口**（检索 navigate/goto/move_to = 0，`goto` 的命中均为清洁机 `CLEANBOT_GOTO_WORKSTATION` 「前往工作站」状态码）。<br>**但机上 PdCoreSDK 有完整导航能力**——见下方更正 | ❌（云端）<br>⚠️（SDK） |
| 在已建图范围内选择**预设点位**（充电桩、指定区域、作业起点） | ✅ 可实现。`GET /map-service/v1/open/point` 取当前地图点位；`GET /map-service/v1/open/group` 取点位分组（**仅 PuduT300**）；派发时指定 `point_id` 或目的地名称 | ✅ |
| 导航过程中实时展示规划路径和当前进度 | ⚠️ **无规划路径下发接口**。可展示实时位置轨迹（`notifyRobotPose`）+ 巡航路径（`get_cruise_line`，**仅 PuduBot2**），但非「规划路径」 | ⚠️ |
| 仅对支持导航 API 的设备开放（擎朗、普渡、联通系列） | ✅ 平台侧按机型白名单控制按钮显隐 | ✅ |

> **⚠️ 重要更正（复核时发现）**
>
> 机上 **PdCoreSDK 的导航能力远强于云端 Open API**
> （见 `zh/PUDU OS SDK/模块能力/007-导航能力.md`）：
>
> | SDK 接口 | 能力 |
> | :--- | :--- |
> | `newDestinationNaviTask` | 点对点任务 |
> | `newGroupNaviTask` / `recycleGroupNaviTask` | 群组 / 回收任务 |
> | `newCruiseNaviTask` | 巡航任务 |
> | `newPatrolNaviTask` + `IPatrolNaviTask.updateFinalGoal(x, y)` | **巡逻任务，且可按坐标更新最终目标** |
> | `newStraightLineNaviTask` + `setStraightLineParam(distance, angle)` | 直线运动，可指定距离与角度 |
> | `newAutoChargingTask` | 自动回充 |
> | `INaviTask.pause()/resume()/stop()/cancelCurrent()/doNext()` | **任务级暂停/恢复/停止，不受云端机型白名单限制** |
> | `globalPauseNavigation/globalResumeNavigation/globalStopNavigation(id)` | **全局暂停/恢复/停止导航** |
> | `poseFl` / `getCurrentPose()` / `speedFl` | 位姿与速度实时流 |
>
> **影响两条结论**：
> 1. `updateFinalGoal(x, y)` 接受坐标参数，**「点击地图坐标移动」在 SDK 层面可能可行**
>    （官方注意事项写「导航目标应来自地图接口返回的数据，**不要手写未知点位**」，
>    说明坐标须落在已建图可达区域，非任意点）。
> 2. `globalStopNavigation` / `INaviTask.stop()` 提供了**比云端「暂停任务」更强的停止能力**，
>    是 F3-2 降级方案的更好选择——但仍**不是急停**（不切断动力，官方明确
>    「调用前确认现场安全」）。
>
> **仍不改变一期判定**，因为 SDK 是**机上 Android 应用内调用**，不是云端可调的 HTTP 接口：
> 要用它必须在每台机器上部署我方 App，再由该 App 反向连接我方平台——
> 等于把管控逻辑下沉到机器人，架构、部署与运维成本完全不同，且需逐机型申请交付包。
> 若客户对"点击地图移动"和"远程停止"为强需求，应作为**二期专项**评估，
> 并先向普渡确认 PdCoreSDK 对本项目 19 台设备的机型覆盖。

**建议验收口径（一期，纯云端方案）**：改为「在地图上选择**已建图的预设点位**下发移动指令，
设备开始移动并实时更新位置，到达后状态变为已到达」。删除「点击任意目标点」与「展示规划路径」。

---

### 5.4 视频监控与回传 — ❌ 普渡设备全章不可实现

**检索证据（对 305 篇官方文档全文检索）**

| 关键词 | 命中数 |
| :--- | ---: |
| RTSP | **0** |
| RTMP | **0** |
| WebRTC | **0** |
| HLS | **0** |
| 推流 | **0** |
| 直播 | **0** |
| 录像 | **0** |
| 视频流 | **0** |
| 摄像头 | **0** |
| 相机 / camera | 37 处 / 11 篇。**云端与私有云侧全部在故障码中**（`LostCamera` 相机数据丢失、`CLEANBOT_FACE_CAMERA_DATA_LOSS` 人脸相机数据丢失）；<br>**但机上 PdCoreSDK 有 `cameraManager()`，提供「相机列表、相机预览、拍照」——见下方重要更正** |
| 拍照 | 1 处 —— `POST /biz-open-service/v1/robotDoor/task_list`「获取舱门拍照」，闪电匣柜内取放物**存证照片**，机型限 FlashBot 2025 / Ultra |

**云端两条路线确认无视频**：公有云 Open API（156 篇）、私有云 Open API（138 篇）——
无任何流媒体协议、推流、录像、云台接口。

> **⚠️ 重要更正（复核时发现，修正本表早期结论）**
>
> 机上 **PdCoreSDK（Android AAR，Kotlin）并非无相机能力**：
> `PdRobotSdk.cameraManager()` 提供 **「相机位置、相机列表、相机预览、拍照」**
> （见 `zh/PUDU OS SDK/SDK概述/001-PdCoreSDK 简介.md`、`004-Demo使用说明.md`
> 「基础能力验证 - 相机：相机列表、相机预览入口」、
> `附录/010-API Reference 使用说明.md`「相机位置、预览、拍照结果 → `camera` 相关包」）。
> BellaPro 机型另有托盘左右相机预览（`005-贝拉Pro.md`）。
>
> **这不改变「平台侧拿不到视频流」的结论，但改变了方案 C 的可行性评估**：
> - SDK 只提供**本机预览与拍照**，**无任何推流/编码/RTSP 接口**（检索 rtsp/rtmp/webrtc/hls = 0）。
>   预览画面停留在机器人本机 Android 界面，不出机器。
> - 但既然 SDK 能取到相机，**理论上可由我方开发一个跑在机器人上的 Android App，
>   取帧后自行编码推流到园区流媒体服务器**。
> - **代价与风险**：需逐机型申请 PdCoreSDK 交付包（含加密版 `adb`，需商务审批）；
>   App 需装到每台机器并设为自启动模块；受机器算力、电量、网络与普渡系统升级影响；
>   `cameraManager` 未公开帧回调格式，能否取到原始帧需**实测确认**（能力文档未写，
>   须查交付包内 `docs/html` 的 `camera` 包签名）；不在普渡质保与支持范围内。
> - **且仅覆盖装得上 PdCoreSDK 的普渡 Android 机型**，我方自研的 4 类特种设备
>   与非 Android 机型不适用（后者接口由我方自行定义，不存在此限制）。
>
> 结论不变：**不建议以此作为一期视频方案**，但澄清件中应据实陈述"SDK 有相机接口、
> 但无推流能力、定制开发可行性待实测"，不宜简单写作"完全无相机"。

| PRD 需求 | 判定 | 说明 |
| :--- | :--- | :--- |
| F4-1 实时视频查看（19 台带摄像头设备，延迟 ≤2 秒，RTSP/RTMP/HLS/WebRTC 适配） | ❌ | 无视频源 |
| F4-2 多画面轮巡与分屏（1/4/9/16 分屏，轮巡间隔可配） | ❌ | 无视频源 |
| F4-3 录像存储与回溯（默认保留 30 天，时间轴回放，倍速，MP4 下载） | ❌ | 无视频源 |
| F4-4 云台与摄像头控制（方向、变焦） | ❌ | 无视频源，亦无云台接口 |

**唯一的图像类产出**：清洁报告中的 `task_result_url` — 清洁覆盖轨迹图（PNG，阿里云 OSS 托管），
可用于 F6-2 清洁报表可视化，但**与视频监控无关**。

**PRD 3.1 设备清单中，7 类普渡设备的「核心能力」栏均写有「视频回传」，此描述与普渡开放平台
实际能力不符。** 另「路面巡逻机器人（360° 视频回传 + 红外测温）」在普渡 26 种机型枚举中
不存在对应产品，该设备的品牌归属很可能有误。

---

### 5.5 视频 AI 智能分析 — ⚠️ 取决于视频源，部分场景可由我方自研设备承载

F5 的输入源是视频流。**在普渡设备上不成立**，但 6 个 AI 场景中有 2 个本就对应我方自研设备：

| PRD 需求 | 判定 | 说明 |
| :--- | :--- | :--- |
| F5-1 人员入侵 / 烟火 / 垃圾满溢 / 异常行为（4 个场景） | ❌ 普渡设备<br>✅ 方案 A | 场景型告警，固定点位 IPC 是更合理载体 |
| F5-1 **管道缺陷识别** | ⚠️ **我方设备** | 输入源为我方管道机器人，**不受普渡限制**，取决于该设备视频接口 |
| F5-1 **水面漂浮物识别** | ⚠️ **我方设备** | 输入源为我方水下 / 水面机器人，同上 |
| F5-2 AI 告警生成与展示（截图、置信度、处理状态流转） | ✅ | 平台侧逻辑，不依赖普渡 |
| F5-3 AI 分析开关与配置（分设备开关、检测时段、置信度阈值） | ✅ | 平台侧逻辑，不依赖普渡 |

**关键结论**：F5-2、F5-3 的平台侧逻辑（告警记录、筛选、状态流转、配置管理）本身不依赖任何厂商，
只要有视频源即可实现。而 6 个 AI 场景中，**「管道缺陷」「水面漂浮物」的输入源是我方自研设备**，
这两项的可行性由我方内部确认即可，**不构成对外依赖**。真正需要客户决策的，
只是剩余 4 个场景的视频源从何而来（即方案 A 的固定点位摄像头）。

---

## 四、视频与 AI 的解决方案（需客户书面选择）

### 方案 A（我方推荐）— 园区固定点位加装 IPC 摄像头

园区固定点位部署网络摄像机（IPC）承担视频监控与 AI 分析，机器人平台只接状态与控制。

**优势**
- **技术成熟**：IPC 标配 RTSP / GB28181 / ONVIF，流媒体侧用 ZLMediaKit 或 SRS 转 WebRTC/HLS，
  录像用 NVR 或流媒体服务器落盘，均为成熟方案
- **AI 效果更好**：固定视角背景稳定，模型误报率显著低于移动机器人的晃动画面
- **需求本质更匹配**：「人员入侵」「烟火」「垃圾满溢」都是**场景型**告警 —— 它们发生在
  固定的地点（周界、配电房、垃圾房），本就该由固定视角持续盯守，
  而不是等移动机器人偶然经过时才可能被拍到
- **PRD 5.4 / 5.5 / 7.1 的全部指标（延迟 ≤2 秒、16 路并发、30 天录像、AI 延迟 ≤3 秒）
  在此方案下均可正常承诺**

**代价**：需追加摄像头、交换机、存储、布线的硬件采购与施工，涉及预算与工期调整。

**需客户明确**：点位数量与位置、是否已有既存监控可复用、预算追加额度。

### 方案 A+（复核后新增，我方最推荐）— 我方自研设备视频 + 园区固定点位 IPC

在方案 A 基础上，**把我方自研的水下 / 爬壁 / 管道 / 灭虫四类设备的视频一并纳入**：

| 视频来源 | 覆盖的 AI 场景 | 依赖方 |
| :--- | :--- | :--- |
| 我方自研特种设备自带摄像头 | 管道缺陷识别、水面漂浮物识别 | **我方内部，无外部依赖** |
| 园区固定点位 IPC（方案 A） | 人员入侵、烟火、垃圾满溢、异常行为 | 需追加硬件采购 |
| 普渡机器人 | 无 | —— |

**这样 PRD 5.5 的 6 个 AI 场景可全部覆盖，验收标准第 4 条无需删除。**
5.4 的实时视频、分屏轮巡、录像回溯亦可在「我方设备 + IPC」范围内正常承诺，
仅需把 F4-1「19 台带摄像头设备」的口径修正为实际具备视频能力的设备数量。

**前置条件**：我方设备团队出具四类机器人的视频协议与拉流方式（见第八章），
建议优先确认，因为它直接决定这个方案的覆盖面。

### 方案 B — 视频与 AI 移出一期

PRD 5.4、5.5 整体移出一期范围，验收标准第 2、4 条同步删除。一期交付状态监控、远程控制、
任务调度、报表、权限五大模块。

**优势**：范围清晰，一期可如期交付，无预算变更。
**代价**：客户核心诉求之一（AI 能力缺失）未被满足，需二期立项。
**说明**：在确认特种设备为我方自研后，本方案的必要性下降 —— 至少「管道缺陷」
「水面漂浮物」两个场景本可在一期落地，整章移出会造成不必要的范围损失。

### 方案 C — 由普渡或集成商定制推流能力

要求普渡开放机器人摄像头推流，或由第三方在机器人上加装图传模块。

**风险**：需普渡产品线排期支持，周期与可行性均不可控；加装模块涉及设备改造，
可能影响原厂质保。**我方不建议将一期交付节点绑定在此方案上。**

---

## 五、其余需求点对照

### 5.6 任务调度与作业统计

#### F6-1 定时任务与排班 — ✅ 可实现

| PRD 要求 | 普渡对应能力 | 判定 |
| :--- | :--- | :--- |
| 创建定时任务（设备、模板、执行时间、起止条件） | 清洁：`GET /cleanbot-service/v1/api/open/cron/list` 读定时任务，`task/exec` `type:10` 控制定时任务开关<br>其他类型：由平台侧调度器在指定时间调用派发接口 | ✅ |
| 任务到点自动派发，记录成功/失败 | 平台侧调度器 + `TASK_STATUS`、`notifyCleanStatus` 回调 | ✅ |
| 任务冲突检测 | 平台侧逻辑 | ✅ |

**说明**：普渡的定时任务能力仅覆盖清洁机（读取与开关），其余机型的定时调度由**平台侧调度器**
实现（到点调用派发接口），这是正常且更灵活的做法。

---

#### F6-2 作业统计报表 — ✅ 可实现（覆盖度良好）

普渡 `data-board` 系列共 37 个统计接口，开箱即用。

| PRD 要求 | 普渡对应接口 / 字段 | 判定 |
| :--- | :--- | :--- |
| 作业时长、次数、完成率、异常次数 | `/data-board/v1/brief/run` → `duration`(h)、`mileage`(km)、`task_count`<br>`/data-board/v1/analysis/run`、`/analysis/run/paging` | ✅ |
| 利用率（作业时长 / 在线时长） | ⚠️ 需平台侧自行计算（在线时长由回调累计） | ⚠️ |
| **清洁专项**：已清洁面积、耗水量、清洁覆盖率 | `/data-board/v1/log/clean_task/query_list`、`/query` → `clean_area`(m²)、`task_area`（计划面积）、`average_area`、`cost_water`(ml)<br>覆盖率 = `clean_area / task_area`<br>另有 `task_result_url` 清洁覆盖轨迹图（PNG） | ✅ |
| 清洁 24 小时运行分布 | `/data-board/v1/analysis/clean/detail` | ✅ |
| 物流/配送：配送次数、准时率 | `/data-board/v1/analysis/task/delivery`、`/task/delivery` 目的地执行明细 | ✅ |
| **物流/配送：载重总重** | ❌ 无载重字段（见 F2-2） | ❌ |
| 巡逻：巡逻里程、点位覆盖率、告警数 | 里程 `mileage` ✅；巡航明细 `/analysis/task/cruise` ✅（**仅 PuduBot2**）；告警数由平台侧累计 ✅ | ⚠️ |
| 按日/周/月查看，导出 Excel / PDF | 统计接口支持时间范围参数；导出由平台侧实现 | ✅ |

**建议验收口径**：删除「载重总重」；「利用率」注明为平台侧计算的派生指标。

---

### 5.7 系统管理

#### F7-1 用户与角色权限 — ✅ 可实现

纯平台侧功能，不依赖普渡。RBAC 模型 + 按功能模块（设备管理/视频监控/远程控制/AI 告警/报表/
系统管理）勾选权限 + 操作粒度细分（查看/控制/导出/配置）均为常规实现。

**注意**：PRD 4. 用户角色表中「运维值班员」「安保人员」的权限含「紧急停止」「视频查看」，
需随 F3-2、5.4 的结论同步调整。

#### F7-2 操作日志与审计 — ✅ 可实现

纯平台侧功能。记录操作人/时间/IP/类型/对象/结果，保留 ≥180 天，支持筛选导出。
日志不可篡改可通过追加写 + 校验链实现。

---

## 六、非功能需求对照（PRD 第 7 章）

| PRD 要求 | 判定 | 说明 |
| :--- | :--- | :--- |
| 7.1-1 支持同时在线全部设备，状态上报并发无延迟 | ⚠️ | PRD 未给具体并发数。19 台规模无压力，但**须向普渡确认 API QPS 限制**（决定清洁机位置轮询频率） |
| 7.1-2 ≥16 路 1080P 视频并发 | ❌ | 依赖视频源，见方案 A/B/C |
| 7.1-3 首屏 ≤3 秒，详情页 ≤2 秒 | ✅ | 平台侧优化，状态数据本地缓存 |
| 7.1-4 AI 推理单路延迟 ≤3 秒 | ❌ | 依赖视频源 |
| 7.2-1 可用率 ≥99.5% | ✅ | 平台侧架构保障 |
| 7.2-2 第三方 API 故障不导致平台崩溃 | ✅ | 适配层熔断降级 + 展示最后已知状态 |
| 7.2-3 视频流中断自动重连 | ❌ | 依赖视频源 |
| 7.2-4 数据每日备份保留 30 天 | ✅ | 平台侧 |
| 7.3 安全性（bcrypt、JWT、凭证加密、日志防篡改） | ✅ | 平台侧。**AppKey/AppSecret 必须加密存储且不明文展示**，符合 PRD 7.3-5 |
| 7.4 兼容性（Chrome/Edge、1920×1080、Linux 部署） | ✅ | 平台侧 |

---

## 七、验收标准逐条复核（PRD 第 9 章）

| # | PRD 原文 | 判定 | 建议修订 |
| :--- | :--- | :--- | :--- |
| 1 | 设备全部接入，状态/位置/电量/任务状态正确展示，刷新延迟 ≤5 秒 | ⚠️ | 补注「位置刷新：支持推送的机型 ≤5 秒，清洁机型为轮询」 |
| 2 | 全部带摄像头设备可实时查看视频，延迟 ≤2 秒，支持 4/9/16 分屏和轮巡 | ❌ | **删除**，或改为「方案 A 加装 IPC 后适用」 |
| 3 | 远程启停、任务派发、急停、导航指令下发成功率 ≥99%，响应 ≤10 秒 | ⚠️ | **删除「急停」**；「导航」限定为预设点位；成功率针对**接口调用成功**而非物理动作完成 |
| 4 | AI 分析覆盖 4 个核心场景，告警生成延迟 ≤3 秒 | ❌ | **删除**，或改为「方案 A 加装 IPC 后适用」 |
| 5 | 作业统计报表数据准确，支持日/周/月导出 Excel | ✅ | 保留（删除载重相关指标） |
| 6 | 用户角色权限配置生效，操作日志完整可追溯 | ✅ | 保留 |
| 7 | 第三方 API 断连时平台不崩溃，对应设备正确标记离线 | ✅ | 保留 |
| 8 | 连续 72 小时稳定性测试无崩溃、无内存泄漏 | ✅ | 保留 |

---

## 八、待客户 / 普渡确认事项

### 需客户确认（阻塞方案定稿）

| # | 事项 | 阻塞内容 |
| :--- | :--- | :--- |
| 1 | **视频与 AI 方案选择 A / B / C** | PRD 5.4、5.5 两章，验收标准 2、4 条 |
| 2 | **急停方案选择 A / B / C** | PRD 5.3.2，验收标准第 3 条，涉及安全责任 |
| 3 | **19 台设备的逐台型号与 SN** | 全表机型白名单判定，标 🔍 的所有条目 |
| 4 | 3.1 表格中 1~7 项品牌是否确为普渡（2.1 与 F3-3 提到擎朗 KEENON、联通） | 若含擎朗/联通，需另行索取其 API 文档重做对照 |
| 5 | 「路面巡逻机器人（360° 视频 + 红外测温）」的实际品牌与型号 | 普渡 26 种机型中无此产品 |
| 6 | 设备种类数：2.1 说「约 12 种」，3.1 合计「11 类 19 台」 | 范围界定 |
| 7 | 7.1-1「支持同时在线全部设备」的具体并发数 | 性能验收口径 |
| 8 | 录像保留天数对应的存储容量（PRD 标注「待确认」） | 仅方案 A 下需要 |
| 9 | AI 准确率 85% 的基线数据来源（PRD 标注「待现场数据验证」） | 仅方案 A 下需要 |

### 需普渡确认（阻塞开发启动）

| # | 事项 | 影响 |
| :--- | :--- | :--- |
| 1 | AppKey / AppSecret 申请与审批周期 | **项目启动前置依赖** |
| 2 | 回调地址能否配置多个 / 后期能否自助修改 | 测试与生产环境隔离 |
| 3 | 开放平台 QPS / 调用频次限制 | 清洁机位置轮询频率，F2-1 刷新指标 |
| 4 | 书面确认「**云端**开放平台不提供视频流能力」 | 澄清件证据 |
| 5 | 是否存在未公开的远程急停接口 | F3-2 结论 |
| 6 | 云端是否支持导航到任意坐标（非预设点位） | F3-3 结论 |
| 9 | **PdCoreSDK `cameraManager` 能否取到原始视频帧**（而非仅本机预览），是否允许自行编码推流 | 决定方案 C/D 是否成立，**须实测或普渡书面答复** |
| 10 | **PdCoreSDK 支持的机型清单**，本项目 19 台中哪些可装 | 二期「点击地图移动 / 远程停止」可行性 |
| 11 | PdCoreSDK 交付包申请流程与周期（含加密版 `adb`） | 二期前置依赖 |
| 12 | 在机器人上部署第三方 App 是否影响质保与官方支持 | 风险评估 |
| 7 | 私有云版本（`/openapi/` 前缀，138 篇文档）的部署要求与费用 | 若客户要求数据不出园区 |
| 8 | 逐台设备的机型枚举值（对应 `product_code`） | 全表机型判定 |

### 我方（国科）自行提供 —— 非对外阻塞项

PRD 3.1 与 6.2 中归属「国科（邓博）」的水下、爬壁、管道、灭虫四类机器人**即我方自研设备**，
其接口能力由我方内部提供，不依赖外部厂商配合，**不构成项目阻塞风险**。
需在方案定稿前由我方设备团队出具下列规格：

| # | 交付项 | 用途 |
| :--- | :--- | :--- |
| 1 | 四类机器人的 API / SDK 接口文档 | 适配层开发依据 |
| 2 | 视频流协议（RTSP / GB28181 / 私有协议）与拉流地址格式 | **决定这 4 类设备能否纳入视频与 AI 范围** |
| 3 | 管道机器人气体传感器（CH₄/H₂S/CO）数据格式 | PRD 5.2.2 特种设备参数 |
| 4 | 可下发的控制指令清单与权限边界 | PRD 5.3 控制功能 |
| 5 | 状态上报方式（主动推送 / 轮询）与字段字典 | PRD 5.2 状态监控、F2-1 刷新指标 |

> **这一点对视频方案的影响，值得客户特别注意**：
> 本表第 5.4 / 5.5 章判定「视频与 AI 不可实现」，其**约束仅来自普渡开放平台**。
> 我方自研的这 4 类特种设备若自带视频能力（水下机器人通常有摄像头），
> **则视频与 AI 可在这部分设备上先行落地**，无需依赖普渡。
> 建议将视频范围重新界定为「我方自研设备 + 方案 A 的园区固定点位摄像头」，
> 而非笼统地整章移出——这既保住了 PRD 5.5 的 AI 场景（水面漂浮物、管道缺陷
> 本就对应我方设备），也让验收标准第 2、4 条有可交付的载体。

---

## 九、附：本表的证据来源

所有判定均可在本地文档镜像中复核：

```bash
cd pudu-docs

# 一键复核本表全部关键结论（推荐）
./verify_claims.py

# 查某个接口
./search.py --path status              # 状态类接口
./search.py --path cleanbot            # 清洁机接口

# 按机型筛选（避开白名单坑）
./search.py --model T300
./search.py --model CC1

# 全部回调通知
./search.py --callback
```

**关于检索方法的说明**：普渡文档含 base64 内联图片，且 front matter 的 `doc_id`
是随机字符串，直接 `grep` 会产生假命中（例如 doc_id `sq0ns40bagptzmc4g6uh9psn`
含子串 `ptz`，清洁机状态码 `CLEANBOT_GOTO_WORKSTATION` 含 `goto`）。
`verify_claims.py` 会先剥离这些噪声再检索，输出如下：

```
=== 结论一：无任何视频流能力 ===
✅ 视频流协议 (RTSP/RTMP/WebRTC/HLS): 0 篇命中
✅ 视频相关中文词 (推流/直播/录像/视频流/摄像头): 0 篇命中
✅ 云台: 0 篇命中

=== 结论二：无远程急停下发接口 ===
   「急停」出现于 7 篇，逐处均为被动能力（故障类型枚举、恢复回调、错误码）

=== 结论三：无信号强度字段 ===   ✅ 0 篇命中
=== 结论四：无载重字段 ===       ✅ 0 篇命中
=== 结论五：无任意坐标导航 ===   ✅ 0 篇命中
```

| 引用文档 | 路径 |
| :--- | :--- |
| 鉴权与网关 | `zh/云开放接口/开发指南/003-鉴权接入.md` |
| 机型枚举（26 种） | `zh/云开放接口/开发指南/FAQ/007-机器类型.md` |
| 故障码字典（351 条） | `zh/云开放接口/开发指南/010-错误码.md` |
| 机器人状态 V2 | `zh/云开放接口/通用接口/机器人信息/机器人通用状态查询/055-获取指定机器人状态V2.md` |
| 清洁机详情 | `zh/云开放接口/通用接口/机器人信息/机器人详情/053-获取清洁机器状态详情.md` |
| 清洁指令 | `zh/云开放接口/机器人任务/清洁任务/078-清洁指令.md` |
| 配送指令（PAUSE 仅 T300） | `zh/云开放接口/机器人任务/配送任务/067-配送指令.md` |
| 急停恢复回调 | `zh/云开放接口/回调通知/机器状态与绑定/017-急停恢复通知-robotEmergencyRecover.md` |
| 位置上报回调 | `zh/云开放接口/回调通知/地图与位置回调/020-机器位置上报-notifyRobotPose.md` |
| 清洁报告详情 | `zh/云开放接口/统计数据/任务明细数据/145-清洁报告-详情.md` |
| 接口总表（含全部机型白名单） | `API-INDEX.md` |

---

**编制**：软件开发项目组
**日期**：2026-08-28
**版本**：V1.0（待客户确认）

> 本表基于普渡官方文档 2026-08-28 快照编制。普渡开放平台持续更新，
> 签认前可执行 `cd pudu-docs && python3 scrape.py` 重新抓取并复核结论。
